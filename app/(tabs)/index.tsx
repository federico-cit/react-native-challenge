import { StyleSheet, Text, View } from "react-native";

export default function HomeScreen() {
  return (
    <View>
      <Text>Render the main list in this screen.</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  //Focus on functionality styles are a bonus
});
